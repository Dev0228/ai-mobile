export type NormalsChartItem = {
  name: string;
  uv: number;
  pv: number;
};

export type LastIncomeItem = {
  name: string;
  value: number;
  color: string;
};

export type LastDataItem = {
  name: string;
  value: number;
  fill: string;
};
